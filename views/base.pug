doctype html
html(lang="en")
  head
    block head
      meta(charset='UTF-8')
      meta(name='viewport' content='width=device-width, initial-scale=1.0')
      link(href='https://fonts.googleapis.com/css?family=Lato:300,300i,700' rel='stylesheet')
      link(rel='stylesheet' href='/css/style.css')
      link(rel='shortcut icon' type='image/png' href='/img/favicon.png')
      //- title Natours | #{title || 'Exciting Journeys'}
      title #{`Natours | ${title}`}
      link(rel='stylesheet', href='https://api.mapbox.com/mapbox-gl-js/v2.12.0/mapbox-gl.css')
      script(defer, src='https://api.mapbox.com/mapbox-gl-js/v2.12.0/mapbox-gl.js')
      script(defer, src="/js/bundle.js")
  body
    // HEADER
    include _header
      
    // CONTENT
    block content
      h1 Place holder content

    // FOOTER
    include _footer